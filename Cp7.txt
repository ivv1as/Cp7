начало
функция qwerty(a,b,c,d):
    если (a>=0 или b>=0 или c>=0 или d>=0):
        если ((c-a)>=1 и (d-b)>=1) :вывод('Да') 
        иначе:
            если ((c-d)>=1 и (d-a)>=1) :вывод('Да') 
            иначе: вывод('Нет')
    иначе:вывод('Числа должны быть целыми и неотрицательными')  
вывод('Введите длину первого прямоугольника') 
try:
    a = int(input()) 
except ValueError:
    вывод('Число должно быть целым и неотрицательным')
    exit() 
try:
    вывод('Введите ширину первого прямоугольника') 
    b = int(input()) 
except ValueError: 
    вывод('Число должно быть целым и неотрицательным') 
    exit()  
try:
    вывод('Введите длину второго прямоугольника')
    c = int(input()) 
except ValueError:
    вывод('Число должно быть целым и неотрицательным') 
    exit()  
try:
    вывод('Введите длинну второго прямоугольника')
    d = int(input()) 
except ValueError:
    вывод('Число должно быть целым и неотрицательным') 
    exit() 
qwerty(a,b,c,d)
конец
